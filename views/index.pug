extends layout

block content
  head
    img.center-block(src='/images/logo.svg' class='logo')

  body 
    div.center-block(class='container-fluid')
      div(class='row')
        div.center-block(class='col-sm-6 associates')
          a(href='roster/associate/create') 
            button.center-block(type="button" class="button") ASSOCIATES
        div.center-block(class='col-sm-6 shareholders')
          a(href='/roster/associates') 
            button.center-block(type="button" class="button") SHAREHOLDERS

